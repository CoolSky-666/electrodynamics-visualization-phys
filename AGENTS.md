# Electrodynamics 可视化项目协作规范（给代码智能体）

本文件用于约束与统一本项目中自动生成/修改代码与文档的风格，确保不同脚本之间的可读性、一致性与可复现性。

---

## 1. 语言与排版

- 对话回复：优先使用中文。
- Python 代码注释与 Matplotlib 图标题/题注：使用英文。
- 保持 Python 代码格式简洁，不用使用过多的函数。
- Markdown 文档：使用中文。
- Markdown 数学公式：
  - 行内公式使用 `$equation$`。
  - 行间公式使用
    $$
    equation
    $$

---

## 2. 命名规范

### 2.1 脚本命名

- 静电学脚本：`e-*.py`（或已有序号体系保持一致）。
- 磁静态脚本：`m-*.py`（或已有序号体系保持一致）。
- 教学/专题示例目录：在相应主题目录下建立子文件夹，例如 `1-static-field/<topic>/`。
- 新增可视化脚本时，尽量用“主题 + 物理量”命名，例如：
  - `magnetic1-B.py`：磁场分布（以 $B$ 为主）
  - `solid_angle_coils.py`：立体角磁标势法（线圈）

### 2.2 输出文件命名

- 每次绘图必须保存图片文件到 `images/` 目录下：
  - 静态图：`.png`，plt.savefig("xxx")
  - 动图：`.gif`
- 推荐输出名与脚本同名，例如脚本 `xxx.py` 输出 `xxx.png`（或主图名包含关键参数）。

---

## 3. 代码风格与结构（静场问题）

静场可视化脚本优先采用如下结构（从上到下）：

1. 参数区：定义几何参数、物理常量、强度参数（电荷/电流/磁矩等）。
2. 理论表达式：优先写出解析形式的电势/磁标势：
   - 静电：$\varphi$ 或 $\phi$
   - 磁静态：$\varphi_m$
3. 网格：统一用笛卡尔网格 `np.meshgrid`（除非问题天然用极坐标/柱坐标）。
4. 物理量计算：
   - 若有解析场表达式，优先直接算 $E$ 或 $B$。
   - 若有解析势函数，使用数值梯度得到场：
     - $\vec{E}=-\nabla \varphi$
     - $\vec{B}=-\mu_0\nabla \varphi_m$（无电流区域）
5. 可视化：用 `plt.streamplot` 绘制场线，并配合几何边界、源位置标注等。

---

## 4. Matplotlib 绘图约定

### 4.1 语言

图中的任何文字（title, legend等）都使用英文表述

### 4.2 场线（streamplot）

- `broken_streamlines=False`（默认保持连续场线）。
- 线色默认黑色：`color='black'`。
- 线宽默认：`linewidth=1`。
- 推荐显式设置 `density`，并根据视野大小微调。
- 推荐使用 `plt.axis('equal')` 保持几何不变形。

### 4.3 颜色与符号（约定）

- 电荷：
  - 正电荷：红色（`red`）
  - 负电荷：蓝色（`blue`）
- 镜像/等效电荷（或等效源）：
  - 若为“镜像正/负电荷”：仍按正红负蓝
  - 若为“标记性辅助”（例如外加场方向）：`darkorange`
- 线圈/导体边界：
  - 边界线默认黑色（`black`）
  - 需要强调时加粗 `linewidth=2`

### 4.4 叠加绘制顺序

- 场线 `zorder=0`
- 几何边界 `zorder=2`
- 源点/箭头/文字 `zorder>=3`

---

## 5. 物理含义与配套文档

- 在文件夹目录的 `README.md` 中编写对应的物理题目：
  - 引用脚本生成的图片 `xxx.png`
  - 用 LaTeX 写出关键推导与最终解析式
  - 给出数值结果与解析结果的对比点（例如中心值/轴上值）

---

## 6. 可靠性与数值稳定建议

- 避免除零：对 $r=0$、源点附近应做掩膜或加小量 `eps`。
- 分段物理解（内外区域、边界条件）优先：先分区再计算，避免在边界附近直接套单一公式。
- 若使用数值梯度：
  - 显式传入网格步长（`np.gradient(phi, dy, dx)`）
  - 必要时对源附近做 `np.nan_to_num` 或掩膜，避免污染流线。